# DeepLens Grafana Dashboard Provisioning
# Automatically imports dashboards for infrastructure monitoring

apiVersion: 1

providers:
  - name: "DeepLens Infrastructure"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards
    updateIntervalSeconds: 30
    allowUiUpdates: true
    foldersFromFilesStructure: true
